'use client'

// src/components/pages/DeGalleryPageClient.tsx
// Thin locale wrapper around the shared GalleryPageClient.

import GalleryPageClient from './GalleryPageClient'

export default function DeGalleryPageClient() {
  return <GalleryPageClient locale="de" />
}
